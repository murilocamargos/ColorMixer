#ifndef _INCLUDES_H
#define _INCLUDES_H

#define MAX(x, y) (((x) > (y)) ? (x) : (y))
#define MIN(x, y) (((x) < (y)) ? (x) : (y))

// Este arquivo contém as bibliotecas padrão que são
// utilizadas em quase todas as classe da aplicação.

#include <string>
#include <vector>
#include <map>
#include <sstream>

#include <stdio.h>
#include <stdlib.h>
#include <math.h>

#include <wx/wx.h>
#include <wx/config.h>
#include <wx/splash.h>
#include <wx/taskbar.h>

#endif

